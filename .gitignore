# Test-only helpers (isolated harness; never commit)
api/gen_local_settings.sh
api/gen_manual.sh
api/run_with_settings.js
api/seed_test.js
api/test_auth.js
api/run_auth_tests.sh
api/auth_test.log
/tmp/*.log
*.log

# Client app config: holds APP_API_KEY for the browser. NEVER commit it —
# it is injected at deploy time by the GitHub Actions workflow from a repo
# secret (APP_CLIENT_KEY). Local dev without this file sends '' and the API
# accepts it (dev fallback in validateApiKey.js).
app-config.json
student list.csv
/reference books PDFs

# --- Agent / dev tooling (not part of the shipped game) ---

# npm dependencies: install via `npm install`; never commit the tree
node_modules/

# Hermes agent scratch workspace
.hermes/

# Qoder IDE auto-generated repo wiki / docs (regenerated locally, not shipped)
.qoder/

# One-off scratch string dump
temp_strings.txt

# Local dev/test throwaway artifacts (logs, screenshots, one-off verify scripts)
boot_refactor_plan.md
boot_refactor_shot.png
boot_refactor_test.js
boot_refactor_test_t8.js
chrome_log.txt
td_verify.js
td_verify2.js
td_verify3.js
test_screenshot.png

# Root-level test/debug screenshots (automation artifacts, not shipped)
/screenshot_*.png
/td_*.png
