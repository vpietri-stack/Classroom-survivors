{
  "version": "2026-09-16c",
  "deployedAt": "2026-09-16",
  "notes": "SR delta sync (2026-09-16c, poisoned account: old account 65KB full-state packet exceeds ~64KB keepalive cap, EVERY flush Failed to fetch). finalizeSession ships only touched entries (extractSRDelta, lastSession===current); server merges delta onto stored state (srDelta flag; absent=legacy full replace). In-memory/persisted full state stays complete. Quota guard persists delta envelope on QuotaExceeded. Speech ?v=11 forces API-bypass deploy. Tests: block 9 (delta fraction/flag/single-entry), delta-merge contract; flush-deadline 84 pass."
}
